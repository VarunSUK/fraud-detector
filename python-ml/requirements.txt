# Core ML libraries
lightgbm==4.6.0
xgboost==3.4.1
scikit-learn==1.7.1
pandas==2.3.2
numpy==2.2.6

# Explainability (SHAP values for real per-transaction feature attribution)
shap==0.48.0

# Model serving and serialization
joblib==1.5.2
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Data streaming
kafka-python==2.0.2
redis==5.0.1

# Development and testing
pytest==7.4.3
pytest-cov==4.1.0
black==23.11.0
flake8==6.1.0

# Jupyter itself is installed separately in the Dockerfile's development
# stage (for ad hoc exploration); no plotting libraries or committed
# notebooks exist yet, so they aren't pinned here -- see docs/ml-pipeline.md
